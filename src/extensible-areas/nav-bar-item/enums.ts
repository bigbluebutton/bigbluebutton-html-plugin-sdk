// Nav bar items types:
export enum NavBarItemType {
  BUTTON = 'NAV_BAR_BUTTON',
  INFO = 'NAV_BAR_INFO',
}

export enum NavBarItemPosition {
  LEFT = 'left',
  CENTER = 'center',
  RIGHT = 'right',
}
